# 拆包和交换

对返回的数据直接拆包

```text
def func():
    a = 1
    b= 2
    c = 3
    return a, b, c

a, b, c = func()
```

* 拆包时注意拆的数据个数要与变量的个数相同，否则程序会异常
* 除了对元组拆包之外，还可以对列表、字典等拆包

交换2个变量的值

```text
a = 1
b = 2
a, b = b, a
```



